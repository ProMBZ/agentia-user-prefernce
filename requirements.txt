# requirements.txt

# For the "langchain_google_genai" integration with Gemini
langchain_google_genai

# For general LangChain usage (sometimes needed for schema, etc.)
langchain

# The LangGraph library for multi-agent state graphs
langgraph

# For environment variable loading
python-dotenv

# For the web UI
streamlit

# For typed dictionaries (backport in some older Python versions)
typing_extensions
